import Flex from '../../Flex.component';

const ModalFooter: React.FC<{ children: React.ReactNode }> = ({ children }) => <Flex flexDirection={'column'}>{children}</Flex>;

export default ModalFooter;
